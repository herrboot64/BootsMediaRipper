# Tools

This folder contains repo setup, build, reporting, and maintenance scripts.

Public scripts must not contain private IPs, real server names, SMB paths, API keys, PINs, or local-only configuration.
